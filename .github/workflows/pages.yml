# Check the pages, then publish them to https://computing-hub.github.io/pythonblocks/
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Let a running deploy finish rather than cancelling it part way.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Test the flowchart core
        run: node tools/test-flowcharts.js

      # vendor/ is not in the repository: refill it from the offline pages,
      # then confirm those pages are still exactly what the online ones build.
      - name: Check the offline copies are in sync
        run: |
          python3 tools/build-offline.py extract
          python3 tools/build-offline.py check

  deploy:
    needs: check
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      # Publish the pages themselves, not the tooling around them.
      - name: Collect the site
        run: |
          mkdir _site
          cp *.html _site/

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v4
